<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="@dimen/padding_list_item"
    android:background="@color/background_color"
    app:cardCornerRadius="@dimen/padding_list_item"
    app:cardElevation="@dimen/padding_list_item">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <TextView
            android:id="@+id/tvName"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentStart="true"
            android:layout_centerVertical="true"
            android:layout_toStartOf="@+id/ivStarship"
            android:fontFamily="@font/raleway_regular"
            android:padding="@dimen/padding_list_item"
            android:textAlignment="center"
            android:textSize="18sp" />

        <ImageView
            android:id="@+id/ivStarship"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentTop="true"
            android:layout_alignParentEnd="true"
            android:layout_marginBottom="@dimen/padding_list_item"
            android:contentDescription="@string/starships"
            android:src="@drawable/ic_starship_large" />

    </RelativeLayout>

</androidx.cardview.widget.CardView>
